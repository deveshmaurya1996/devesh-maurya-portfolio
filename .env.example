# Optional — chat works without this via local portfolio answers.
# When set, NIM may polish phrasing (short timeout) then falls back locally.
# Model is chosen dynamically from the NVIDIA catalog (no NVIDIA_MODEL needed).
NVIDIA_API_KEY=
