# Optional — chat works without this via local portfolio answers.
# When set, NIM may polish phrasing (2.5s timeout) then falls back locally.
NVIDIA_API_KEY=
NVIDIA_MODEL=mistralai/mistral-nemotron
# Optional comma-separated extras for NIM failover
# NVIDIA_MODELS=
